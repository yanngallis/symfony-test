{% extends 'base.html.twig' %}

{% block title %}Symfony Test{% endblock %}

{% block body %}
<div class="example-wrapper">
    <h1>Symfony Test Application</h1>
</div>

<div>
            {% if payment=='success' %}
				<p style="border: green solid thin">You purchase is validated!</p>
			{% elseif payment=='cancel' %}
				<p style="border: red solid thin">You purchase is cancelled ...</p>
	        {% endif %}

    {% for product in products %}
        <h2>{{ product.title }}</h2>
        <h3>{{ product.price }} €</h3>
        
        <a href="{{ path("buy_product", {'product_id': product.id }) }}">Buy this product</a>
    {% endfor %}
</div>

{% endblock %}
